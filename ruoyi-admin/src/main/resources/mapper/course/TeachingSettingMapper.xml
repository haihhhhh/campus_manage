<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.course.mapper.TeachingSettingMapper">
    
    <resultMap type="TeachingSettingModel" id="TeachingSettingResult">
        <result property="gradeName"    column="grade_name"    />
        <result property="gradeCode"    column="grade_code"    />
        <result property="courseName"    column="course_name"    />
        <result property="courseCode"    column="course_code"    />
        <result property="teacherName"    column="teacher_name"    />
        <result property="teacherCode"    column="teacher_code"    />
        <result property="season"    column="season"    />
    </resultMap>

    <sql id="TeachingSettingVo">
        select bg.grade_name,bg.grade_code,course_name,ci.course_code,teacher_name,ct.teacher_code,ci.season from base_grade bg
        left join course_info ci on bg.grade_code=ci.grade_code
        left join course_base cb on cb.course_code=ci.course_code
        left join course_teaching ct on ct.course_id=ci.id
        left join base_teacher bt on bt.teacher_code=ct.teacher_code
        GROUP BY bg.grade_code,ci.course_code,ct.teacher_code,ci.season
    </sql>

    <select id="selectTeachingSettingist" parameterType="TeachingSettingModel" resultMap="TeachingSettingResult">
        <include refid="TeachingSettingVo"/>
        <where>
            <if test="season != null  and season != ''"> and ci.season = #{season}</if>
        </where>
        ;
    </select>
    
<!--    <select id="selectCourseInfoById" parameterType="Long" resultMap="CourseInfoResult">-->
<!--        <include refid="selectCourseInfoVo"/>-->
<!--        where id = #{id}-->
<!--    </select>-->

<!--    <insert id="insertCourseInfo" parameterType="CourseInfo" useGeneratedKeys="true" keyProperty="id">-->
<!--        insert into course_info-->
<!--        <trim prefix="(" suffix=")" suffixOverrides=",">-->
<!--            <if test="courseCode != null">course_code,</if>-->
<!--            <if test="gradeCode != null">grade_code,</if>-->
<!--            <if test="season != null">season,</if>-->
<!--         </trim>-->
<!--        <trim prefix="values (" suffix=")" suffixOverrides=",">-->
<!--            <if test="courseCode != null">#{courseCode},</if>-->
<!--            <if test="gradeCode != null">#{gradeCode},</if>-->
<!--            <if test="season != null">#{season},</if>-->
<!--         </trim>-->
<!--    </insert>-->

<!--    <delete id="deleteCourseInfoById" parameterType="Long">-->
<!--        delete from course_info where id = #{id}-->
<!--    </delete>-->

<!--    <delete id="deleteCourseInfoByIds" parameterType="String">-->
<!--        delete from course_info where id in -->
<!--        <foreach item="id" collection="array" open="(" separator="," close=")">-->
<!--            #{id}-->
<!--        </foreach>-->
<!--    </delete>-->

</mapper>